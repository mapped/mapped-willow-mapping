[project]
name = "mapping-gaps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["networkx", "fastapi", "uvicorn", "python-multipart"]
